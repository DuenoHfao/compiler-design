# Compiler Design

Went from `Compiler Design` to `Full System Design`

## References
All references are in the `references/` directory.

## CPU
Exist in `cpu/` directory.

## Compiler
Exist in `compiler/` directory.

## OS
Exist in `os/` directory.